const numbers = [4, 9, -1, 16, -5, 25];

const convertNumbers = () => {
    let raiz = numbers.map((number) => {
        return Math.sqrt(number);
    }) 
    return raiz;
}

console.log(convertNumbers(numbers));